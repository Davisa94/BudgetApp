{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean and build",
            "type": "shell",
            "command": "make",
            "args": ["clean", "all"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/src/"
            }
        }
    ]
}